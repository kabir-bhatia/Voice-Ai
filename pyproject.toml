[project]
name = "v5"
version = "0.1.0"
requires-python = ">=3.12"
# Fully self-hosted — NO Google plugin. STT/LLM/TTS all via openai-compatible localhost.
dependencies = [
    "livekit-agents[silero]>=1.6",
    "livekit-api>=1.1.0",
    "livekit-plugins-noise-cancellation~=0.2",
    "livekit-plugins-openai>=1.6",
    "python-dotenv>=1.2.2",
]
